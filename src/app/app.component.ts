import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<router-outlet></router-outlet>', //me permite establecer routing en cualquier lugar de angular
 
})
export class AppComponent {
  
}
